[target.wasm32-unknown-unknown]
runner = "echo" # do not attempt to run.
rustflags = [
  "-C", "link-args=--import-memory",
  "-C", "link-args=-L .", "-C", "link-args=-lmain", # main.c
  "-C", "link-args=-L../musl-1.2.2/lib", "-C", "link-args=-lc", # musl libc
]

[build]
target = "wasm32-unknown-unknown"

[net]
offline = true
